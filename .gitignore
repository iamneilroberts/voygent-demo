node_modules
dist
.dev.vars
.wrangler
dist-web

# one-time fixture capture raw dumps (may contain prod trip detail)
scripts/_fixtures-raw/
.env
.env.*

# Local embedding model cache (huge; never commit)
.fastembed_cache/
