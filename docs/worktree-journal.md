# Worktree Journal

Active and recent isolated worktrees for this repo. Agents starting new
sessions should review this. Mark entries `done` with `/branch done <slug>`
when the work ships or is abandoned.

## Active

(none)

## Coordination

Cross-cutting constraints active across sessions. Format:
`<YYYY-MM-DD HH:MM> — <slug>: <constraint>. <expires when or unblock condition>.`

- 2026-06-11 15:55 — (main) funnel-resmoke → all sessions: **Phones now default to the claude skin — SHIPPED + DEPLOYED + PUSHED** (`origin/main` `24c5076`, live bundle `index-S1R7yA2g.js`, 477 tests, tsc clean; client-only). Neil's mobile smoke showed the board (amber) skin is broken on phones (header/split-flap title overflow, composer cramped, Inspector eats 70vh; C7 mobile pill still deferred). Neil's call: claude-on-phones now, board mobile polish later. `resolveSkin(param, stored, smallScreen)` — ≤760px forces claude, IGNORING a stored "board" preference; explicit `?skin=board` still wins (escape hatch for testing). Desktop precedence unchanged. **Board-skin mobile CSS pass = open follow-up (post-promotion).** NEEDS NEIL RE-SMOKE: reload demo.voygent.ai on the phone → should land in claude skin.
- 2026-06-11 09:00 — (main) funnel-resmoke → all sessions: **Public-funnel re-smoke COMPLETE via demo.voygent.ai — promotion is GO pending Neil's browser eyeball.** Verified headless against prod: statics 200 (`/`, `/blog`, `/stats`, `/folio-updates/folio-client` 145716B, bundle `mUB0ysTN`); auth-gate negatives pass (`/auth/me` no-cookie 401, `/onboard` bad-origin 403 / empty-name 400); **self-serve onboarding works end-to-end** (`POST /onboard` → 200 `{ok,code}`, Resend email CONFIRMED received by Neil); code → `/auth` → public-tier cookie → `/auth/me` 200; **LIVE leg ran a real off-menu `flight_search` (BOS→LIS) on the NEW credential-free bearer** — genuine TAP fares ($1,521/pp TP 218, real flight numbers), SSE streamed incrementally (`source`/`text`/`folio`/`savings` events present), $0.22/exchange on Sonnet; `POST /pro-request` → 200 + Neil notify email. **PR #6 deploy decision: settled — nothing left to deploy** (PR head `4277e96` ∈ main; `origin/main` == local == deployed). Bearer swap done by voygent-lite (`VOYGENT_MCP_BEARER` = verified credential-free identity; `VOYGENT_MCP_BEARER_PRO` unset → pro fails closed by design). ⚠️ NOTE: `/admin/*` is unreachable from CLI (CF Access 302s before the worker's ADMIN_TOKEN fallback — `scripts/demo-admin.sh` is broken against prod). **Remaining before promoting the URL (Neil, in-browser):** (1) eyeball reel → end-CTA → onboard form → live w/ public banner; (2) `/admin` requests queue has a pending smoke pro-request ("Smoke Test Pro") — GRANT it and use the minted code to confirm pro 503 fail-closed (then revoke), or just deny it; (3) promote. A smoke public code (`sbzm…`, emailed to Neil, $2/day) is live for the eyeball run.
- 2026-06-10 22:05 — (main) inspector-declutter → all sessions: **Funnel-noise + per-day-chip polish SHIPPED + DEPLOYED + PUSHED.** `origin/main` == `216c19b` (== deployed), live bundle `index-mUB0ysTN.js`, 472 tests, tsc clean, /folio-updates still 200/145716. (1) **Funnel rows** now hide distills below `MIN_DISTILL_PCT=20%` (kills the `flightSearch 700→692 −1%` noise; hotels/flight_list still show), and `funnelAggregateRows` EXCLUDES `searchDistill` (it was double-shown as a row AND an aggregate slice). Client-only. (2) **patch_trip itinerary chips** no longer all read "Building the day-by-day" — `toolChipTitle` inspects the day array (model patches the FULL array per layer): dining→"Adding restaurants to your days (N days)", activities→"Adding activities to your days (N days)", scaffold→"Setting up the day-by-day (N days)". Worker-affecting (`shared/tool-chip-title.ts` via `loop.ts`); **live path only** — reel events carry no args so the reel keeps the generic label. Deferred richer "what's on each day" tier (needs per-activity naming) NOT done (Neil chose the easy one). **NEEDS NEIL SMOKE:** live Tokyo/Cancún trip — funnel shows only real wins, itinerary chips now distinct.
- 2026-06-10 21:45 — (main) inspector-declutter → folio-pub + all sessions: **/folio-updates RESTORED + 3 more fixes deployed.** Rebased my 7 local commits onto `origin/main` 33d69f1 (PR #7 folio-updates — clean, additive), rebuilt, redeployed → `demo.voygent.ai/folio-updates/folio-client` now **200 / 145716 B** (was clobbered to 2 B by my earlier deploys), advisor 200/146646, index 200/2523. Live bundle still `index-DvcsFqZR.js` (no code drift; this deploy just re-added the statics). **Fixes in this bundle (beyond the declutter):** (1) drillable summary tiles now read as clickable (they were `background:none` so the inert tiles looked more active — amber caret + lift-to-`board-3` on hover/open); (2) Pro-window % now uses FRESH tokens (input+output), not cumulative cost-weighted — old line claimed a trip ate 537% of a Pro 5-hr window (backwards); (3) **excursion_search crash FIXED** (`session-do.ts` searchDistill block force-unwrapped `meta!` for non-search measurements → `ERROR: Cannot read properties of undefined (rawTokensEst)` surfaced as a TOOL result; now guards `m && meta`); (4) smoke harness (`scripts/smoke-enriched-run.mjs`) now FAILS the run on any tool ERROR result (would have caught #3). **✅ PUSHED — `origin/main` now at `950dce0` (== deployed; clean FF over 33d69f1).** Trunk matches the live bundle, so other sessions can `git pull --rebase origin main` and build without re-clobbering. (The earlier clobber was caused by deploying from unpushed local main — now resolved by pushing.) **NEEDS NEIL SMOKE:** Cancún/Tokyo trip — tile click affordance, Pro-window %, excursion search no longer errors.
- 2026-06-10 21:08 — (main) inspector-declutter → all sessions, esp. demo-auth-redesign: **Aggressive Inspector declutter SHIPPED + DEPLOYED to prod** — main `9edb7e8`, live bundle `index-D3vLSb-P.js`, 470 tests, tsc clean. Asset-only deploy (no worker/secret/D1 touched, builds on top of `SouVgFP9`). The Engineering Inspector "Live this session" region is now stripped to **pipeline strip + "view critical path" + tool-call log** only; ALL other always-visible detail (cost-sim, savings/funnel, observer-effect overhead, phase-machine trail, store-ops, trip-integrity, cross-session aggregates) relocated **behind the summary drill tiles** as drill content, read off a single pre-computed `DrillContext`. New drills: `stores` (persisted-writes tile) + `integrity` (validation tile, now a drill button). New pure transform `funnelAggregateRows` (filters aggregate savings to tokensSaved>0, kills the "patch · 0" wart). Removed dead `fmt`/`usd`/`showCost` + `ValidationSection`/`StoreOpsWidget` standalone renders from Inspector.tsx. **Edge served the new bundle WITHOUT a CF purge this time** (cache HIT but `must-revalidate` revalidated against new origin; verified `/` references `D3vLSb-P` + asset 200). Sessions building from main: `git pull` before editing `Inspector.tsx` / `web/src/lib/inspector-drills.tsx` / `inspector-stats.ts`. **NEEDS NEIL SMOKE:** `?mode=live&skin=claude&advisor=1` Cancún trip → expand Inspector → click each tile (incl. persisted-writes → stores, validation → integrity) + "view critical path".
- 2026-06-10 19:55 — (main) demo-fix3 → ALL SESSIONS, esp. demo-auth-redesign: **demo-auth-redesign is now MERGED + DEPLOYED to prod** (Neil's call, demo not yet in use). Live bundle `index-SouVgFP9.js`, 468 tests, tsc clean. Done this session: (1) merged the branch to main (`3a72780`); (2) applied D1 migrations to prod — DEMO_DB `0003_tier`/`0004_onboarding`/`0005_pro_requests`, STATS_DB `0002_stats_code_id`; (3) set `DEMO_PUBLIC_LIVE_ENABLED="1"` + `NEIL_NOTIFY_EMAIL` in wrangler.toml, set `RESEND_API_KEY` secret (from voygent-lite); (4) wired the folio-board **"Get an auth code for a live demo →" CTA** (claude `FolioArtifact` + board `FolioPanel`, live sessions only) → existing `ProAccessForm` → `/pro-request`; (5) folio FINISH-step prompt fix (no more publish/subdomain dead-end). `VOYGENT_MCP_BEARER_PRO` left UNSET (pro access fails closed — pro-request captures leads + emails Neil, but granted pro codes 503 until the secret is set). **🚨 PRE-PROMOTION TODO (security): swap `VOYGENT_MCP_BEARER` to a credential-free identity BEFORE promoting the demo URL** — self-serve onboarding now opens public live on that bearer (same bearer the passcode demo already used, but the gate is now a form not a passcode). **To demo-auth-redesign session:** the rest of Phase D (D1–D3, Z1, admin polish) is still yours; main is deployed, build from it. **Credential-free-bearer task → voygent-lite handoff `~/dev/voygent-lite/docs/summaries/handoff-2026-06-10-credential-free-demo-bearer.md`** (provision a free-tier credential-free identity, hand its bearer back, demo runs `wrangler secret put VOYGENT_MCP_BEARER`). **Edge-cache note:** post-deploy, CF served a stale `/` (cache HIT, bundle `DX88RvfJ` instead of `SouVgFP9`); the voygent-lite CF token lacks Cache-Purge perm — Neil to purge `https://demo.voygent.ai/` from the CF dashboard (or it clears on TTL).

- 2026-06-10 18:45 — (main) inspector-polish → all sessions: **Phase D /code-review fixes SHIPPED + DEPLOYED**, live bundle `index-DX88RvfJ.js`, 436 tests, tsc clean. High-effort review (7 finder angles) caught: (1) waterfall drill double-rendered when "view critical path" opened — summary accordion now filters to `trigger.kind==="stat"`; (2) fanout emit gated on `isIntercepted` alone (missed the `hotel_search_and_rank` alias + could re-emit stale fanout on a faithful/live bypass) — now `!bypassReplay && (isIntercepted || name==="hotel_search_and_rank")`; (3) funnel rows keyed by tool → collide on two hotel searches, now tool+index; (4) restored the `≈` cue on the "context kept out" tile. Deferred (consistent/minor): drill-tile-at-0 opens an empty-state (same as funnel/cost-sim, informative). Cleanup backlog (quality, not done): triplicated tool→stage map, duplicate usd/fmt formatters x3, drill-shell markup dup, WaterfallView recomputes gantt total.
- 2026-06-10 18:30 — (main) inspector-polish → all sessions: **Phase D Stage 4 (Supplier Fan-Out) SHIPPED + DEPLOYED — Phase D COMPLETE (all 4 stages)**, live bundle `index-_c-zgffe.js`, 436 tests, tsc clean. New **`fanout` inspector event** (`shared/events.ts`): replay records which supplier adapters a hotel search/list aggregated (`worker/mcp/replay.ts` `setHotelFanout`/`lastFanout`, reset alongside `measurement`), session-do emits it (out-of-band, model payload unchanged). Hotels are **genuinely multi-source** — fixtures already carry serp `hotels[]` AND `cpmaxxHotelsFor()`, so cpmaxx (credentialed) + serp/Google both light up with REAL captured counts. New **`suppliersQueried` drillable tile** → **Supplier Fan-Out drill** (`inspector-drills.tsx` `fanoutGroups`/`litSupplierIds`/`FanoutView`): per consolidated call → lit provider chips (real counts) → "deduped to N", plus the dimmed `SUPPLIER_CATALOG` (`web/src/inspector-data.ts`, 12 real adapters) with depth/breadth popups, BTS-style disclaimer. App.tsx accumulates `fanouts` like `savings`. **Phase D done: funnel + cost-sim + waterfall + fan-out.** Remaining Phase D backlog (later registry/drill entries, not started): no-cache/no-distill cost scenarios (vestige `582538d6`, needs server repricing), flight fan-out (needs Kiwi/lastminute/Expedia captures), per-turn token waterfall, model-routing swimlane, fabrication-guard ledger. NEEDS NEIL SMOKE: all 4 drills on a Cancún live trip.
- 2026-06-10 18:11 — (main) inspector-polish → all sessions: **Phase D v1 (drill-down telemetry) SHIPPED + DEPLOYED**, live bundle `index-DQteSEWE.js`, 430 tests, tsc clean. New **drill registry** `web/src/lib/inspector-drills.tsx` (DrillContext + DRILLS + pure transforms funnelRows/costScenarios/ganttBars) extends the Phase C stat registry; the open-panel summary tiles are now **registry-driven + drillable (C5 folded in)**. Three drills: (1) **Token Elimination Funnel** on the "context kept out" tile — per-search `raw→slim` bars + "what the model actually saw" payload; needed one additive field on the searchDistill savings event (`shared/events.ts` + `worker/session-do.ts:543` emit `rawTokens/slimTokens/tool`, model payload unchanged). (2) **Counterfactual Cost Simulator** on the "observed cost" tile — actual vs all-Sonnet vs all-Opus bars (no-cache/no-distill DEFERRED, server-side repricing, vestige intention `582538d6`). (3) **Per-phase Critical-Path Waterfall** — "view critical path" under the pipe block; sequential gantt by stage. Spec `docs/superpowers/specs/2026-06-10-inspector-phase-d-drilldown-telemetry-design.md`, plan `…/plans/2026-06-10-inspector-phase-d-drilldown-telemetry.md`. **NEXT: Stage 4 — Supplier Fan-Out** (specced, real multi-source hotels from existing serp+cpmaxx fixture data + lit/dimmed provider drill; new `fanout` event; detailed plan tasks not yet written). NEEDS NEIL SMOKE: the three drills on a Cancún live trip.
- 2026-06-10 17:10 — (main) inspector-polish → all sessions: Phase C (skinny live Inspector rail) SHIPPED, live bundle `index-D4HAjykg.js`. Inspector NO LONGER auto-grabs half the screen: `EngState` now `idle|peek|open` (`web/src/lib/inspector-state.ts`); first tool → a live ~96px `peek` rail (active phase + pipeline pips + top-3 registry stats + attention beat); click → full `open` two-pane; collapse returns to rail. Extensible stat registry `web/src/lib/inspector-stats.ts` (buildStats/railStats/deepDiveLinks) drives rail + the panel-bottom "Dig deeper" links (stat-tied primary + "More on the engineering" secondary). 421 tests, tsc clean. DEFERRED to follow-up: C5 (panel tiles from registry — internal refactor, panel still shows same stats), C7 (mobile live-indicator button), Phase D (drill-down telemetry: token funnel, cost simulator, critical-path waterfall — own plan). NEEDS NEIL SMOKE: the peek rail (live at rest, attention beat on first tool, click-to-expand, dig-deeper links). See lines below for Phases A/B.
- 2026-06-10 17:01 — (main) inspector-polish → all sessions: Phase B (self-describing tool chips) SHIPPED, live bundle `index-Cw6inXvV.js`. Chips now read human labels ("Searching hotels in Cancún", "Shortlisting hotels", "Building the day-by-day") + raw tool mono tag; shared resolver `shared/tool-chip-title.ts`. 417 tests.
- 2026-06-10 16:55 — (main) inspector-polish → all sessions: WORKER REDEPLOYED, new live bundle `index-KiVaahoc.js` (was `BLCqyX_j`). Phase A (price fixes) of the inspector-rail+surface-polish plan SHIPPED: hotel cards/folio headline the CLIENT price with "All-inclusive · N nts · M travelers" context; per-night reconciles from the headline; advisor ladder only shows "below public $X" on real savings; flights show "N travelers · $X each". 414 tests, tsc clean. Spec `docs/superpowers/specs/2026-06-10-inspector-rail-and-surface-polish-design.md`, plan `docs/superpowers/plans/2026-06-10-inspector-rail-and-surface-polish.md`. NEXT: Phase B (self-describing tool chips), Phase C (skinny live Inspector rail). Also still TODO from before: cpmaxx 3-up hotel multi-select (`handoff-2026-06-10-cpmaxx-multiselect.md`). Clears once Neil smokes Phase A prices.
- 2026-06-10 13:45 — reel-r5-screenplay → all sessions: ✅ **Option 1 (hide manage_trip_goal) NOW APPLIED + garbled working-indicator FIXED** (bundle `index-BVJzW2xj.js`, WORKER). Neil smoked a Cancún menu trip: STILL ran live + skipped boards because the model used `manage_trip_goal` (real Voygent driver) to self-drive past the intercepted replay flow → latched live. With faithful now default-off, Option 1 finally bites: hide `manage_trip_goal` (CHECKLIST_DRIVER_TOOLS) from the tool catalog when `!faithful && !liveMode` → featured trips must use save_trip/flight_search → matches fixtures (replay), phase machine steps (present-and-wait at FLIGHT_PICK/HOTEL_PICK), boards render. Re-exposed in faithful mode or once a session latches live (off-menu, turn 2+). Also: the live working indicator was STILL garbled (one-word-per-line) — `.cl-thinking span` dot rule outranks `.cl-thinking-label` reset on specificity (extra element selector); scoped it to `.cl-thinking:not(.cl-thinking-live)`. Also removed the mid-reel "build your own" mobile pill (flow = reel→auth→live via end CTA). 403 tests, tsc clean. **NEEDS NEIL RE-SMOKE: Cancún/NYC menu trip should now replay (fast, "Sample results" tag), show flight board → pause → hotel board → pause, and the working indicator should read as one clean line.**
- 2026-06-10 13:10 — reel-r5-screenplay → all sessions: ✅ **ROOT CAUSE FOUND + FIXED: public demo was in FAITHFUL mode** (bundle `index-C6s_xNjQ.js`, WORKER changed). The live-demo flood / no-boards / real-$ / ≈0-savings was all ONE cause: `FAITHFUL` + `FAITHFUL_PUBLIC_OK` secrets are both set, so public sessions ran fully faithful → `bypassReplay` unconditional (no replay, all live, 32s searches), `suppressOrchestration` (no phase machine/boards/stepping → flood), `measureSearchDistill=!faithful` (context-kept-out ≈0), seed=real MCP instructions (model used `manage_trip_goal`). Option 1 (hide manage_trip_goal) was MOOT under faithful. **FIX (Neil's call — "dial faithfulness back or make it a URL param"): client now drives faithful per-session, default OFF** = orchestrated (replayed featured trips + boards + stepping + real searchDistill savings; off-menu trips still latch live); **`?faithful=1` opts into fully-live**. Public-safety gate preserved (requested-faithful only honored under FAITHFUL_PUBLIC_OK/test). No-faithful-field clients (tests) keep env default → 403 tests unchanged. This REVERTS public to the original well-tested orchestrated mode (faithful was layered on later) so lower-risk. **NEEDS NEIL LIVE SMOKE: run a menu trip (e.g. the NYC preset) in `mode=live` — should now replay fast/cheap, show flight+hotel BOARDS, step (present-and-wait), and show real context-kept-out. Off-menu still live. `?faithful=1` for a fully-live run.** Honesty touch DONE (bundle `index-DQ4Y2TR9.js`): worker emits a one-time `source` event on first search (live=real supplier data / sample=curated fixtures); UI shows a tag under the ribbon ("Live results…" / "Sample results — featured trip uses curated data, try off-menu for a live run"). Reel shows no tag. Also note: cost figure = LLM list-price only (no supplier/infra); context-kept-out = searchDistill+patch savings (real on replay path).
- 2026-06-10 12:30 — reel-r5-screenplay → all sessions: ✅ **Mobile pick-mark + LIVE streaming feedback SHIPPED + DEPLOYED** (bundle `index-BN-w8B8t.js`, WORKER changed too). (a) mobile "{Actor} chose this" mark dropped to its own full-width line (was off the right edge / blended in). (b) **Live demo "responds all at once after a long wait"**: added `cache-control: no-transform` + `x-accel-buffering: no` to the `/chat` SSE response (`worker/session-do.ts`) so the edge streams events incrementally instead of buffering the whole agent run. The loop already emits tool-start BEFORE each (slow) tool, so once unbuffered the running chips (with the elapsed timer shipped earlier) should appear live. **NOTE: real Voygent backend latency is unchanged (a live hotel_search took 32s in Neil's run) — that's the upstream MCP, not fixable here.** (c) live working indicator: bare pulsing dot → elapsed counter + "running live searches, up to a minute" after 5s (`WorkingIndicator`, live mode only; reel keeps the dot). **NEEDS NEIL RE-TEST: the SSE anti-buffering can't be verified headless (auth-gated stream); if it STILL arrives all-at-once, the cause is upstream (provider.stream / real-Voygent passthrough not yielding incrementally) and needs deeper work.** 403 tests, tsc clean.
- 2026-06-10 12:10 — reel-r5-screenplay → all sessions: ✅ **Mobile + copy + interactive end-state SHIPPED + DEPLOYED** (bundle `index-Bv2Slhpa.js`, `origin/main` HEAD). (a) **Mobile callout placement** — callouts now drop to the opposite vertical half when no side room (no longer cover the target), and tall targets (flight board) align to their TOP so the chosen option + badge stay visible; mobile eng-peek moved to upper area. (b) **Mobile folio callouts** — inline folio was `display:none` on mobile so day-by-day/edit/send callouts floated unanchored; now shown during the reel (`.cl-folio-inline.in-reel`). (c) **Handoff simplified** — dropped the fake 'client reply' card at the send beat (was 3 stacked notices); single clean 'sent' notice, the real reply arrives via the Act 9 routed-back comment. (d) **Callouts reworded** — plain copy, no 'X not Y', no triads, no em-dashes (also de-em-dashed dialogue + end-card). (e) **Commission call-out** — forces advisor mode in the reel, commission on the 3 hotels + booked Dean, closing spotlight on the trip-commission total (11 callouts now). (f) **Interactive end-state** — `ReelExplore.tsx` replaces the end card on collab: viewer swaps hotel / toggles add-ons → live total (seeded from the final client-view snapshot); 'Send to Voygent' → dialog to the live demo + standing CTA + Replay; honest 'scripted demo trip' note kept; other reels keep the end card. 403 tests, tsc clean, client-only. **NEXT: Neil smokes mobile (callout placement, inline folio, eng-peek) + the interactive end-state + commission callout.**
- 2026-06-10 11:28 — reel-r5-screenplay → all sessions: ✅ **More live-smoke fixes SHIPPED + DEPLOYED** (bundle `index-BMVPng16.js`, `origin/main` `2bfbfd2`). (a) **Live demo "felt hung" FIXED** — running tool chips now show elapsed seconds ("Working… 12s"), an indeterminate sweep bar, + a reassurance line after 6s (`ClaudeToolChip` timer; reduced-motion handled). This was the real product bug (long real tool calls looked dead). (b) **Tab title** → "Voygent — interactive demo". (c) **Folio includes** → collapsible long-form (`<details>`/`<summary>`, caret, body on expand; collapsed by default). (d) **Engineering callout reworked**: the reel never populates the inspector + it's single-pane, so the old `stat:distinctTools` callout centered with broken "panel on the right" copy. Neil's call (after honesty tradeoff: collab's cost/token stats would be FAKE) = **brief mini engineering-view peek**. New snapshot-driven `engpanel` interaction (mirrors R4 clientview): `s.agent.engPanel(snapshot|null)`, `reelView.engPanel`, `ReelEngPanel.tsx` slide-in panel (`data-reel-target=eng-panel`) listing the REAL tools so far, NO cost numbers, footnote "full metrics in the interactive demo". Act 4: folio callout → eng panel opens + callout → closes (engpanel dwell 600ms). 403 tests, tsc clean, client-only. **NEXT: Neil smokes — eng-peek placement/timing, live tool-status feedback on a real off-menu run.**
- 2026-06-10 10:55 — reel-r5-screenplay → all sessions: ✅ **R5 follow-up batch SHIPPED + DEPLOYED** (bundle `index-COtgWhG1.js`, CSS `index-BE2jSfZk.css`, `origin/main` `0568488`). Four Neil tweaks from live smoke: (1) **flight pick is the advisor's** now (`s.client.picks`→`s.advisor.picks` for the flight; was wrongly "Client chose this"); (2) **flight options badged** (Voygent tags: Best value/Cheapest/Quickest; hotels: Best location/Best value/Waterfront) + **expandable routing** (legs + layovers + aircraft; picked option auto-expands) — new `BoardCandidate.legs`/`badge` + `FlightLeg` type in `shared/events.ts`, rendered in `BoardView.tsx`; (3) **trip folio compacted** to a summary panel — glance chips (✈/🏨/🗓/ℹ), merged flights+hotels section, one-line-per-day inline activities, includes as chips, commission in header; all reel targets intact. 403 tests, tsc clean, client-only. **Engineering-panel callout: BUILT + DEPLOYED** (bundle `index-C5YEj0QQ.js`, `origin/main` `d00502a`). Neil picked tool-call orchestration. One honest spotlight in collab Act 4 anchored on `validate_trip` → `stat:distinctTools` (the tool count IS real on the scripted reel; cost/token stats are NOT, so the copy points only at the tool orchestration). 10 callouts now. ReelCallout already resolved `stat:<key>`; gracefully centers if the stat is offscreen on narrow viewports. **NEXT: Neil smokes the full batch — flight expand/badges, compact folio, advisor-pick label, eng callout placement + dwell calibration.**
- 2026-06-10 10:35 — reel-r5-screenplay → all sessions: ✅ **R5/5 SHIPPED + DEPLOYED — REEL REWRITE COMPLETE.** Full collab screenplay live on `?reel=collab`. bundle `index-z3_GIW5z.js` (CSS unchanged). Rewrote `dublin-collab.screenplay.ts` into the ~30-beat arc: loose brief that firms up (advisor forgets dates → Voygent asks → advisor adds a requirement) → 6 flights the **traveller** picks (`s.client.picks`) → 3-hotel shortlist the **advisor** curates (`s.advisor.picksMany`, drops the chain) → day-by-day + `validate_trip` → advisor edit + note → **includes** chooser (`s.advisor.picksMany` on `board("includes")`) → `sendsToClient` → **client window**: 5 `s.client.view(...)` snapshots (pick Dean + toggle transfers add-on → total animates 3920→5096→5216), note typed, closes → routes back as a client comment → Voygent adds Temple Bar food tour → final folio. **Makes R2 (multi-select) + R4 (live-priced client view) visible.** Two small touches the handoff anticipated: `clientview` dwell floor 4200ms in `pacing.ts`, `clientview` added to spotlight matcher union in `highlights.ts`. End-card CTA → **"Build your own trip"**. Registry recap/duration(~4min)/framing refreshed, honest scripted-walkthrough framing kept. Hotel only commits in FINAL folio (no flicker). 403 tests (+0 net; rewrote grounding test — 5 interaction kinds, price math, 9 callouts), tsc clean, client-only. **NEXT: Neil smokes `?reel=collab` end-to-end — calibrate pacing/callout density + the client-view price-recalc dwell (4200ms × 5 snapshots ≈ 21s on the window; tune if too long).** No open reel roadmap items.

- 2026-06-10 09:50 — reel-r4-clientview → all sessions: ✅ **R4/5 SHIPPED + DEPLOYED: client-view (simulated browser window + live pricing + book-progress) — capability, invisible until R5.** bundle `index-DDQmK8W1.js`. New `ReelClientSession` + `clientview` interaction (snapshot-based; reducer sets `reelView.clientView`); pure `computeTripTotal` = flights + chosen hotel + activities + toggled add-ons (Neil's pricing model) + `usd()` (tested); `ReelClientView.tsx` = chromed `voygent.app/t/...` window (traffic-lights, locked URL, "simulated" tag, ready-to-book progress, hotel radios w/ price, add-on checkboxes, animated total, client note, Send feedback); App overlay when `clientView.open`; DSL `s.client.view(snapshot|null)`; `data-reel-target='client-view'`. **Not wired into the live collab reel yet — R5 emits the clientview beats.** 401 tests (+7), tsc clean, client-only. **NEXT: R5 — full ~30-beat screenplay rewrite wiring R2/R3/R4 (advisor↔Voygent↔client, multi-select hotels→client picks one w/ live pricing→email→paste→final folio + 'build your own trip' CTA), dwell calibration. Makes R2+R4 visible.**
- 2026-06-10 09:25 — reel-r3-send → all sessions: ✅ **R3/5 SHIPPED + DEPLOYED: folio "Send to client" button + email popup toast.** bundle `index-DBw3bL-P.js`. FolioArtifact footer shows a "Send to client" button (reel mode only, via new `reelMode` prop) that flips to "✓ Sent to client" on `reelView.handoff.sent`; `data-reel-target='folio-send'`; pure `sendButtonLabel()` (+test). ReelHandoffNotice moved from centered overlay → top-right popup toast (clear of the playback bar). **Visible on the collab reel now.** 394 tests (+1), tsc clean, client-only. **NEXT: R4 (client-view = simulated browser window + live pricing + book-progress + client pick/question/send) — the big/novel phase; designing with Neil → then R5 full screenplay rewrite.**
- 2026-06-10 09:05 — reel-r2-multiselect → all sessions: ✅ **R2/5 SHIPPED + DEPLOYED: multi-select picks + 'includes' chooser (capability, no visible change yet).** bundle `index-Csxy2iPS.js`; collab reel behaves identically (single picks wrap to 1-element arrays). Pick interaction now carries `candidateIds: string[]` (`ReelViewState.selected` + `pickedActor` membership); screenplay DSL gains `picksMany(boardId, ids[], echo, folio?)` (compiler validates every id); new `includes` board kind (shared events + BoardItem + BoardView). Model: advisor shortlists ~3 hotels → R4 client view makes it the client's single choice w/ live pricing. 393 tests (+5), tsc clean, client-only. **NEXT: R3 (folio 'Send to client' button + email-popup→paste loop) → R4 (client-view simulated browser window + live pricing + book-progress) → R5 (full screenplay rewrite).**
- 2026-06-10 08:45 — reel-r1-terminology → all sessions: ✅ **R1/5 of the reel rewrite SHIPPED + DEPLOYED: purged user-visible "agent" → "Voygent."** `origin/main` → (this), bundle `index-Dj5nWkxm.js`. `actorLabel('agent')='Voygent'`; header subs → "travel-planning assistant"; folio empty state, send-notice routing chip, collab intro/end-card blurbs + live callout copy all drop "agent". Code (`s.agent` DSL, `Actor`) unchanged; engineering deep-dive "agent" content + "coding-agent workflows" footer left (different context). 388 tests, tsc clean, client-only. **Reel-rewrite plan (Neil-approved): R1 terminology ✅ → R2 multi-select + "includes" chooser → R3 folio "Send to client" + email→paste loop → R4 client-view (SIMULATED BROWSER WINDOW + live pricing + book-progress) → R5 full ~30-beat screenplay rewrite + CTA.** Building phased/in-order, each shipped+smoked. Cast: User=advisor(terracotta), Client=traveler(slate-teal), AI=Voygent.
- 2026-06-10 08:35 — reel-frame-counter → all sessions: ✅ **Reel frame-number readout + toggle SHIPPED + DEPLOYED (client-only, 6 lines).** `origin/main` → `ecd8f31`, bundle `index-BaF0FWLh.js`. A `#` toggle in the playback bar shows `frame/total` (off by default, localStorage-persisted) to reference exact spots. No player/test change. **NEXT (big, designing now): full reel sequence rewrite** per Neil's ~30-beat script (advisor↔Voygent↔client) + new surfaces (multi-select hotel/includes choosers, a client-view interface with live pricing + book-progress, email-paste feedback loop) + purge user-visible "agent" wording. Will be decomposed into phases (spec + plan first).
- 2026-06-10 08:05 — reel-scrubber → all sessions: ✅ **Reel draggable scrubber SHIPPED + DEPLOYED (client-only).** `origin/main` → `ddd7ffa`, Worker `voygent-demo` redeployed (bundle `index-CUAFpmrk.js`, CSS `index-BiQHEsRS.css`). Base `2c42179` = `origin/main` (no rebase). Verified live: `/`=200, `/blog`=200, `/stats`=200; scrubber markers in JS+CSS. Adds drag-to-seek to the playback bar. Reel is stateful, so **seek = reset + fast-forward**: `replayChat` gained `opts.seekTo` (applies frames `[0,seekTo)` instantly — no delay/dwell/pause/callout — then plays on). App overlays a transparent `<input type=range>` on the progress track (native drag + keyboard, accessible); release maps %→frame → `seekToFrame` (abort → resetReelState → seekFrameRef → bump `replayNonce` to re-trigger the replay effect). Touched `recording.ts`(+test), `App.tsx`, `skin-claude.css`. **No worker/secret/D1 change.** 388 tests green, tsc clean. Branch FF-merged; ref retained. ⏳ Neil to smoke drag-seek on `?reel=collab`.
- 2026-06-10 07:50 — demo-faithful-boards-playback → all sessions: ✅ **Live-demo chooser fix (WORKER) + reel playback transport SHIPPED + DEPLOYED.** `origin/main` → `b4f1bd8`, Worker `voygent-demo` redeployed (bundle `index-C2Rlqvv8.js`, CSS `index-DxZxSMRw.css`). Base `d4e8245` = `origin/main` (no rebase). Verified live: `/`=200, `/blog`=200, `/info/*`=200, `/stats`=200, `/auth/me`=401 (gate healthy); playback markers in JS. **⚠️ This deploy includes a WORKER change to the public FAITHFUL path** (not asset-only): (1) **Faithful boards fix** — in live/faithful mode the model followed Voygent's core and called `list_render`, which builds a hosted chooser page on the advisor **subdomain** (external URL) → nothing rendered in-demo + it asked for a subdomain. Fix is **demo-side, NOT a subdomain/demo-user**: strengthened `FAITHFUL_BOARDS_NOTE` (the only board guidance in the faithful seed) to forbid `list_render` and name the in-chat tools (`flight_search`→`flight_list action:list`; `hotel_search_and_rank`/`hotel_search`) so the demo renders choosers in its own BoardView (boards.ts). No subdomain, no demo user, no secret/D1 change; `update_advisor_profile` stays denylisted. Real data/tools unchanged — only render surface stays in-demo. (2) **Reel playback transport** — new top-right control bar (pause/play, restart, progress bar, existing 1×/2×); player gained `opts.pauseGate` (abort-safe) + `opts.onProgress`; `ReelCallout` honors `paused` (no auto-resume while paused). Client-only for that part. Touched `worker/session-do.ts`(+test), `web/src/lib/recording.ts`(+test), `App.tsx`, `ReelCallout.tsx`, `skin-claude.css`. 387 tests green, tsc clean. Branch FF-merged; ref retained. ⏳ Neil to live-smoke an OFF-MENU trip (boards now render in-chat?) + the reel pause/scrub.
- 2026-06-09 20:45 — reel-p4-multiact → all sessions: ✅ **reel P4 SHIPPED + DEPLOYED — the reel P1–P4 effort is COMPLETE.** `origin/main` → `28c315f`, Worker `voygent-demo` redeployed (bundle `index-BmmhFha8.js`, CSS unchanged `index-DWaVHopb.css` — **content-only**). Base `fdb7df4` = `origin/main` (no rebase). Verified live: `/blog`=200, `/info/*`=200, `/stats`=200 (superset, your work intact); P4 content markers in deployed JS. **Expanded the short collab reel into the full multi-act "Dublin, built together" story** (brainstormed w/ Neil; keeps reel id `collab`, stays in rotation, scripted + honest framing). **7 acts, one spotlight callout each:** intake (advisor brief → save_trip) → flights (traveler picks) → hotels (traveler picks) → itinerary (agent builds day-by-day) → refine (advisor edits Day 3 in place) → review (advisor sends to client, reply routes back) → finalize (client comments on Day 6, advisor resolves, food tour added). Traveler picks BOTH flight+hotel (slate-teal); advisor curates+routes (terracotta). Callouts bound by `interactionKind`/`eventType`+`nth` (2 picks disambiguated nth:1/nth:2), all 7 resolve (grounding-tested). **Content-only — no new components/CSS/DSL**; hotel boards, hotel picks, multi-day folios, edits, comments, send-notice all already rendered from P1/P2. Clean folio progression withFlight→withHotel→withDays→edited→withFinal (no flicker). Registry recap chips + duration (~3 min) updated; honest scripted intro/end card kept. Dwells unchanged (calibrate via smoke). Touched `dublin-collab.screenplay.ts`/`registry.ts` + rewrote grounding test; new spec `docs/superpowers/specs/2026-06-09-reel-p4-multiact-collab-design.md`. **No worker/MCP/`ServerEvent`/secret/D1 change.** 384 tests green, tsc clean. Branch FF-merged; ref retained. **Reel roadmap P1–P4 all shipped.** ⏳ Pending Neil's visual smoke of `?reel=collab` (esp. the act pacing/callout density on a ~3 min run).
- 2026-06-09 19:55 — reel-p2-4-finish → all sessions: ✅ **reel P2.4 SHIPPED + DEPLOYED — P2 (collab reel) COMPLETE.** `origin/main` → `91e77fe`, Worker `voygent-demo` redeployed (bundle `index-DbTOTEVd.js`, CSS unchanged `index-DWaVHopb.css` — no new CSS). Base `bbc08dc` = `origin/main` (no rebase). Verified live: `/blog`=200, `/info/*`=200, `/stats`=200 (superset, your work intact); P2.4 markers in deployed JS. **Finalized the collab reel + made its framing honest:** (a) authored the final clean `dublin-collab.screenplay.ts` — 3 flight candidates, dated coherent days (Day 6 = Temple Bar so the food tour fits), plain copy; **fixed the folio flicker** (picks() already emits the folio, so the redundant `folio(draft)` re-emit that dropped the just-picked flight is gone); softened the pick spotlight off the "real fare from live search" claim. (b) **Honesty (Neil's call: "rotate, honest framing"):** collab is a SCRIPTED workflow walk-through, not a real recording like dublin-oct, so the "real session / nothing scripted" intro+end card would mislead. Made BOTH cards reel-aware via per-reel `ReelEntry.intro/endCard/recap` overrides — **dublin-oct keeps the default "real session" copy** (it IS real); collab gets "Built together" intro + "scripted walk-through" note, "How a trip comes together" end card ("your own run pulls real live flights and hotels"), and recap chips (picked together / advisor edits / sent to client / client commented). `collab` was ALREADY in the round-robin rotation (registry.ts); it's now appropriate for it. Dwell floors left at spec values (pick 3.5/edit 3.2/comment 4.2/handoff 5.2s) — visual calibration is Neil's smoke. New grounding test (4 interaction kinds, end-state folio has picked flight + food tour, no flicker, all spotlights resolve) + registry honesty guards. Touched `dublin-collab.screenplay.ts`/`registry.ts`/`ReelIntro.tsx`/`ReelEndCard.tsx`/`App.tsx` (+2 tests). **No worker/MCP/`ServerEvent`/secret/D1 change.** 383 tests green, tsc clean. Branch FF-merged; ref retained. **P2 done end-to-end** (spine→pick/edit→comment/send→content+honesty). Next reel work = P4 (full multi-act Dublin screenplay), separate. ⏳ Pending Neil's visual smoke of `?reel=collab` (and the collab intro/end cards).
- 2026-06-09 19:10 — reel-p2-3-comment-send → all sessions: ✅ **reel P2.3 (comment thread + send-to-client RENDER) SHIPPED + DEPLOYED.** `origin/main` → `6c94243`, Worker `voygent-demo` redeployed (bundle `index-C3EhY9gB.js`, CSS `index-DWaVHopb.css`). Base was `efe49c7` = `origin/main` (no rebase needed). Verified live: `/blog`=200, `/info/*`=200, `/stats`=200 (superset, your work intact); P2.3 markers confirmed in the deployed JS+CSS. **Renders the last two P2 interactions at `?reel=collab`** (data already flowed from the P2.1 reducer; this is rendering only, client-only, claude-skin): (3) **collapsible comment thread** — a count-badge pin under the anchored folio day (actor-colored by lead commenter) that pulses + auto-expands when a comment lands then tucks back; expanded thread = actor-attributed comments w/ colored avatars. New pure `threadsForDay`/`actorInitial` in `reel-render.ts` (+tests); `data-reel-target=comment-<threadId>`. (4) **generic email-style send notification** — neutral mail icon, **no Gmail branding**, a **"simulated"** tag on each notif (Codex honesty flag + Neil): notif-sent + notif-reply + an explicit **"client's note → routed back to the agent"** chip. New `ReelHandoffNotice.tsx`, App-level overlay gated `mode==="auto" && reelView.handoff?.sent`; `data-reel-target=handoff-notice`. Pacing (comment 4200/handoff 5200) + actor tokens already existed — reused, **no pacing/token change**. Touched `App.tsx`/`ClaudeChatView.tsx`/`skin-claude.css`/`reel-render.ts(.test)` + new `ReelHandoffNotice.tsx`. **No worker/MCP/`ServerEvent`/secret/D1 change** — still disjoint from your `worker/info/*` -v2 work. **Live mode untouched** (empty threads/handoff → nothing renders). 377 tests green, tsc clean. Branch FF-merged; ref retained. **NEXT: P2.4** — author final calibrated collab content, end-card recap chips, dwell calibration, **put `collab` in rotation** (still `?reel=collab`-only today). 'viewing as client' ribbon deferred (spec "may"). Plan: `docs/superpowers/plans/2026-06-09-reel-p2-3-comment-send.md`. ⏳ Pending Neil's visual smoke.
- 2026-06-09 18:35 — inspector-board-polish → all sessions: ✅ **Engineering-display + board-selection fixes SHIPPED + DEPLOYED.** `origin/main` → `a6074f9`, Worker `voygent-demo` redeployed (bundle `index-KXLGpDgg.js`, CSS `index-6vKfQvqG.css`). **Rebased onto your `d5013b0` /blog journal commit first — superset** (verified live: `/blog`=200, `/info/*`=200, `/stats`=200, nothing clobbered; my branch already contained `7d18de6` so it carries all your /blog + voice work). Four fixes from Neil's live smoke of the engineering inspector: (1) **board selection now displays** in the default reel / live — the folio-fallback resolution set `resolved:true` but never `resolvedId`, so every card dimmed with no ✓; now resolves to the candidate the agent actually staged/promoted (ids are unambiguous; titles aren't — all flights are "MOB→DUB") via new pure `web/src/lib/board-match.ts` + a `stagedBlob` ref accumulating patch_trip/promote_* args. **Collab reel unaffected** (no pick tools → `resolvedId` stays undefined; P2.2 interaction pick still owns the selection). (2) **Distill node lights up** — driven off the `searchDistill` savings event (flight_list/hotel_list never fire). (3) **Pipeline settles when done** — the traveling packet stopped pulsing forever; now rests + nodes go green once `hasFolio && !busy`. (4) **Inspector reorg** — scoreboard above a fixed-height auto-scrolling tool log (no longer pushes sections down); model-routing condensed to one note + two-up phase selectors. Touched `App.tsx`/`Inspector.tsx`/`styles.css` + new `board-match.ts(.test)`. **No worker/MCP/`ServerEvent`/secret/D1 change** — disjoint from your `worker/info/*` -v2 work, so safe to keep shipping. 374 tests green, tsc clean. Branch `inspector-board-polish` FF-merged; ref retained. ⏳ Pending Neil's visual smoke.
- 2026-06-09 18:20 — deepdive-voice-rewrite → all sessions: ✅ **SHIPPED — /blog landing + deep-dive voice rewrites (ongoing).** `origin/main` → `7d18de6` (superset of reel P2.2 `246ee39`), Worker `voygent-demo` redeployed. New worker-rendered **`/blog`** (auto-indexes content.json entries flagged `blog:true`, client-side search/tags, editable hero `blog-home` via the existing /info editor). **Shared-file heads-up:** `worker/info/content.json` entries may now carry optional `blog`/`tags` (ignored by /info render); `worker/index.ts` gained a `GET /blog` route + imports `collectPosts/renderBlog` from new `worker/blog/render.ts` and `PAGES` from info/pages; `worker/info/layout.ts` crumbs gained a `/blog` link; `scripts/info-content.mjs mergeOverrides` now preserves non-edited fields (blog/tags) on pull. Also new `/info/<slug>-v2` in-voice companion pages (`context-economics-v2` is the first blog post). **No worker-loop/MCP/ServerEvent/secret/D1 change.** 368 tests green. ⚠️ **The main CLONE working tree was checked out on `inspector-board-polish`** (another session); I advanced `main` by ref (`git branch -f`, no checkout) and deployed from my worktree, so that tree is untouched — don't assume the clone is on `main`. Worktree `deepdive-voice-rewrite` stays ACTIVE (more `-v2` pages coming).

- 2026-06-09 17:36 — reel-p2-2-render → all sessions: ✅ **reel P2.2 (pick + edit RENDER) SHIPPED + DEPLOYED.** `origin/main` → `246ee39`, Worker `voygent-demo` redeployed (bundle `index-Gp2D0t63.js`, CSS `index-BQNKt5e4.css`). Rebased onto your `a214171` journal commit first — superset. **First VISIBLE P2 phase:** at `?reel=collab` the board pick now shows an actor-colored selected state + "✓ {Actor} chose this", and the edited folio activity shows a before→after marker + "{Actor} edited" tag (advisor=terracotta `#c96442`, client=slate-teal `#2f7d8c`). New `web/src/lib/reel-render.ts` (pure, tested helpers); touched `App.tsx`/`ClaudeChatView.tsx`/`BoardView.tsx`/`skin-claude.css`. **Live mode unaffected** (no pick → board renders as before; live `edits` is `[]`). Canonical folio still owned by `ServerEvent` folio. No worker/MCP/`ServerEvent`/secret/D1 change. 359 tests green, tsc clean, prod 200. Branch `reel-p2-2-render` merged (FF); worktree removed; ref retained for Neil. **NEXT: P2.3 (comment thread + email-notif send-to-client), then P2.4 (content+integration).** Default reel is still dublin-oct; collab not in rotation (P2.4). Spec: `docs/superpowers/specs/2026-06-09-reel-p2-screenplay-interactions-design.md`.
- 2026-06-09 16:50 — reel-p2-spine → all sessions: ✅ **reel P2.1 SPINE SHIPPED + DEPLOYED.** `origin/main` advanced to `b24a517`, Worker `voygent-demo` redeployed (web bundle `index-B-SEXSOa.js`). Rebased onto your `1c4bbc0` /info content commit first — superset. **Client-only, logic-only, NO rendering yet** (interactions don't display until P2.2). Added: reel-only `interaction` `Frame` kind + `Actor`/`ReelInteraction` (`recording.ts`); pure `applyInteraction` reducer over `ReelViewState` (`interaction.ts`, holds NO folio — canonical folio still owned by `ServerEvent` folio); post-apply interaction dwell (`pacing.ts`); `replayChat` interaction playback + extended highlight matcher targeting interaction frames (`Map<number,Highlight[]>`, backward-compat with `eventType`); screenplay DSL + compiler with lowering validation (`screenplay.ts`); a proof reel at `?reel=collab` (`dublin-collab.screenplay.ts`, NOT in rotation). **No worker/MCP/`ServerEvent`/secret/D1 change.** 354 tests green, tsc clean, prod 200. Branch `reel-p2-spine` merged (FF); worktree removed; branch ref retained for Neil to delete. Next: P2.2 rendering (plan TBD). Spec: `docs/superpowers/specs/2026-06-09-reel-p2-screenplay-interactions-design.md`; P2.1 plan: `docs/superpowers/plans/2026-06-09-reel-p2-1-spine.md`.
- 2026-06-09 14:08 — reel session → all sessions: ✅ **reel + inspector polish DEPLOYED** (`origin/main` = `dfbfa67`, bundle `index-Tr7XLMwJ.js`). Two rounds shipped after reel-p1: (a) inspector polish — distinct-tools-vs-calls split, `data-stat` hooks, compacted model/view/theme control line; (b) **anchored callouts** — reel highlight callouts spotlight their exact target via a fixed overlay + DOM targeting (`data-stat` on Inspector cards, `data-reel-target` on boards/folio-sections/tool-chips, `target` key per highlight), now 8 callouts (flights, hotels, context-saved, excursions, daily-itinerary, value-add, cost, self-correction). Client-only (web/src reel + Inspector + skin-claude.css + styles.css). No worker/secret/D1 change; superset of prior prod. 331 tests green.
- 2026-06-09 12:05 — info-content-portback → all sessions: ✅ **SHIPPED — /info content moved to a regenerable seed + port-back tooling.** `origin/main` = `0b53a6a`, Worker `voygent-demo` redeployed. **Refactor heads-up:** `/info` page content now lives in `worker/info/content.json` (NOT inline in `pages.ts`); `worker/info/resume.ts` was **deleted** (folded into content.json). `pages.ts` exports unchanged (getPageData/mergeOverride/renderInfo/isKnownSlug/infoPageHtml) and rendered output is byte-identical, so no behavior change — but if you edit /info copy, edit `content.json` (or use the live editor + `node scripts/info-content.mjs pull`). `vitest.config.ts` include now also matches `scripts/**/*.test.mjs`. `worker/info/.pull-snapshot.json` is gitignored. No D1 schema/secret change. Rebased onto your reel-plan commit (`806188a`) first — superset. Runbook: `docs/runbooks/info-page-editing.md`.
- 2026-06-09 11:30 — info-inplace-edit → all sessions: ✅ **SHIPPED — in-place /info page editing.** `origin/main` = `124445c`, Worker `voygent-demo` redeployed (bundle unchanged: `index-DELqCcei.js` — this was a worker-only change). **DEMO_DB schema changed:** new additive table `info_page_overrides` (migration `migrations/0002_info_overrides.sql`, **already applied to remote**) — harmless to other features; if you rebuild a fresh DEMO_DB, re-apply 0002 after 0001. `GET /info/*` now sends `cache-control: no-store` (was `public, max-age=300`) so edits show instantly. New routes `POST /info/:slug/save|revert` are admin-gated (reuses `adminAuthed`; I exported it + `AdminEnv` from `worker/access/admin.ts`). No secrets changed (`ADMIN_TOKEN` was already set). No touch to chat/agent/MCP/session. Superset over Plan A.
- 2026-06-09 11:58 — demo-subagent-deepdives → all sessions: ✅ **SUPERSET DEPLOY shipped on top of faithful Plan A.** `origin/main` advanced to `6fe655b` and Worker `voygent-demo` redeployed (web bundle `index-DELqCcei.js`). This is a **rebase-not-clobber** deploy: branch was rebased onto your `09fa8cc` first, so it ⊇ everything you shipped (FAITHFUL code + secrets untouched — I changed **no secrets/vars**; `FAITHFUL`/`FAITHFUL_PUBLIC_OK` remain set). Changes are **/info content + Inspector/INFO_NAV wiring only** — new `/info/subagents` (coming-soon) + `/info/trip-integrity`, context-economics additions, and 3 new engineering-view deep-dive links. No touch to the agent loop, MCP, session DO, or replay. Live-verified all `/info/*`=200. If you deploy next, you already have my work (it's in `origin/main`).
- 2026-06-09 15:10 — faithful-plan-a → all sessions: **Plan A (faithful thin-client keystone) DEPLOYED to prod** (Worker `voygent-demo` version `6e6c5668`, 100%) + `origin/main` updated to `8113e9e`. Deploy is **FLAG-OFF** (`FAITHFUL` unset) so prod behavior is byte-identical to before; new code (MCP `initialize()`, `FAITHFUL`-gated seed/tool-path/loop, `SessRecord.faithful`) is dormant. **No secrets/vars changed.** Note: the 08:45 note below is **STALE** — `APP_ORIGIN` is `https://demo.voygent.ai` (not workers.dev) and has been since `b0c9fc4`; verified live (`demo.voygent.ai`=200, `workers.dev`=404, Origin guard correct→401/wrong→403). ⚠️ **Do NOT set `FAITHFUL`/`FAITHFUL_PUBLIC_OK` secrets** — real supplier tool-call spend is NOT metered yet (see `docs/superpowers/plans/2026-06-09-faithful-plan-a-keystone.md` "Flag-enablement gate"). Rollback if ever needed: redeploy prior version, or just leave the flag unset (it already is).
- 2026-06-09 15:40 — faithful-plan-a → all sessions: **UPDATE — budget gate WAIVED by Neil for current scale; public live faithful APPROVED.** Neil accepts unmetered real-supplier spend at the planned (low) scale — alerts/limits are set and access-token holders are welcome to use it freely. The supplier-key isolation concern (demo shares prod SerpAPI/TripAdvisor/Viator keys — confirmed in voygent-lite, those keys are global-shared) is deferred to **issue iamneilroberts/voygent-lite#179**, to revisit only if driving more public traffic. So `FAITHFUL`/`FAITHFUL_PUBLIC_OK` are now **OK to set** — but enable in this order: (1) set `FAITHFUL=1` and run the Task 6 smoke (admin/`x-demo-test` only — public still gated), verify the live MCP returns `instructions`, the model drives `manage_trip_goal`, the friendly failure chip works, and the folio renders from real `read_trip`; (2) only then set `FAITHFUL_PUBLIC_OK=1` to turn on public live faithful. Not yet enabled as of this note.
- 2026-06-09 16:05 — faithful-plan-a → all sessions: ✅ **ENABLED + SMOKE-VERIFIED in prod. Public live faithful is ON.** Set secrets `FAITHFUL=1` and `FAITHFUL_PUBLIC_OK=1` on Worker `voygent-demo` (both confirmed in `wrangler secret list`). Live smoke via a real passcode against `demo.voygent.ai` with an OFF-MENU trip (Reykjavik, BOS→KEF) confirmed all Plan A behaviors: the model drives `manage_trip_goal` (live-instructions core — the old path never called it), real `flight_search` returned genuine Icelandair fares ($1,167/pp, real flight numbers/seats), real folio rendered, the friendly degradation chip ("trying another…") fired on a transient and recovered, and the live checklist present-and-wait worked. `DEMO_PHASE_MACHINE` is still set but faithful forces it off by design. **Rollback:** `wrangler secret delete FAITHFUL_PUBLIC_OK` (public → flag-off) or also delete `FAITHFUL` (admin/test → flag-off too).
- 2026-06-09 08:45 — improve-demo → auth-tokens session: 🔔 **TRIGGER MET + already actioned.** `main` is reconciled, clean, and **I already ran the Task 1 deploy** (committed `main` → prod, Worker `voygent-demo`). Verified live: `/stats`=200, `/info/phase-machine`=200, `/auth/me`=401, `/presets` has model-selector data; **`APP_ORIGIN = https://voygent-demo.somotravel.workers.dev` is live and the Origin guard works** (correct Origin → 401 needs-auth, wrong Origin → 403). So the "old origin → 403 every POST" problem is **resolved**. ⚠️ I deployed the COMMITTED tree as-is — **no secret/var changes**. If your Task 1 intended a DIFFERENT `APP_ORIGIN` (e.g. a custom domain for CF Access) or new secrets (`CODE_HASH_KEY`/`SESSION_SIGN_KEY`/`ADMIN_TOKEN`), those are NOT applied yet — you still need your deploy for those. Otherwise prod is current. **Branch cleanup done:** all merged branches deleted (local + remote); only `main` remains. Over to you for **Task 2 (CF Access)** — that's CF dashboard config, not code, so it's yours/Neil's. Ping if you need a re-deploy after a var/secret change and I can run it.
- 2026-06-09 08:30 — improve-demo: ✅ **RESOLVED — `main` reconciled and pushed (`origin/main` = `5f0083e`).** `demo-enrichment` (enrichment/phase-machine/multi-provider/stats) and `demo-access-control` (passcode gate, per-code D1 budgets, admin) are now BOTH in `main`. The 5 conflicts (`App.tsx`, `sse-client.ts`, `worker/index.ts`, `worker/session-do.ts`, `wrangler.toml`) were resolved keeping both features — auth gate intact, `/chat` requires a valid session cookie, enrichment routing/phase/stats preserved, both D1 bindings (`DEMO_DB`+`STATS_DB`) kept, both `0001_*` migrations coexist (different DBs). 263/263 tests green (incl. `auth-routes`, `chat-admission`, `access/*`); tsc clean; web bundle builds. **NOT yet deployed** — prod still runs the prior uncommitted combined build; deploying `main` now is safe (it ⊇ prod). `demo-access-control` worktree closed (branch fully merged, ref preserved). Session model: client no longer sends `sessionId` (cookie-based sid); stats sessionId degrades to "anon" — flag if that matters.
- 2026-06-08 — demo-ui-redesign: **MERGED to `demo-enrichment` (`6f11dcb`) + DEPLOYED as a SUPERSET** (Worker `voygent-demo`, served at voygent-demo.somotravel.workers.dev). The UI redesign (10-sec summary strip + Trip-Integrity validation + cost-language relabel + light-mode terminal harmonization + Voygent-owned chat skin + Dublin itinerary date fix) is live. ⚠️ I first deployed the redesign branch alone (based on `1812216`) which momentarily reverted `/info/phase-machine`; caught it, merged `demo-enrichment` (your `acef57b`, incl. `16827fc`) into the branch keeping BOTH features (resolved App.tsx/Inspector.tsx/shared events/styles conflicts), redeployed `6f11dcb`. **Phase machine fully intact** (`16827fc` is an ancestor; `DEMO_PHASE_MACHINE=1` secret untouched; `/info/phase-machine`=200; 222 tests green). `demo-enrichment` FF'd to `6f11dcb`. Branch `demo-ui-redesign` worktree retained.
- 2026-06-08 — phase-machine: **MERGED to `demo-enrichment` (`16827fc`) + DEPLOYED + FLAG LIVE in prod** (Worker `voygent-demo` version `11577554`, `DEMO_PHASE_MACHINE=1` secret set; rollback = `wrangler secret delete DEMO_PHASE_MACHINE`). Added a server-side trip-build state machine in SessionDO (`worker/agent/phases.ts` + additive `afterToolBatch`/`continueDirective` hooks in `loop.ts`; flag-gated wiring in `session-do.ts`; `kind:"phase"` inspector event + a "Workflow engine" trail in `Inspector.tsx`). The phase-machine↔llm-tweaks merge conflicts (App.tsx/Inspector.tsx/index.ts) were resolved keeping BOTH features (StoreOps + phase trail; DeepSeek + DEMO_PHASE_MACHINE env). 221 tests green. **demo-ui-redesign: rebase onto `16827fc` before deploying** — your next deploy of `demo-enrichment` must include this or it reverts the phase machine. `BOARDS_WORKFLOW_OVERRIDE` is untouched (the machine suppresses its own directive at boards pick phases and lets the seed drive). Phase machine is for FEATURED trips; keep it OFF for live/off-menu (v1 limit — directives aren't liveMode-aware). Detail: `docs/summaries/handoff-2026-06-08-phase-machine.md`.
- 2026-06-06 18:50 — claude-skin: SHIPPED — FF-merged to `main` (8ed367c → 821f44e) and deployed to prod. demo-enrichment: rebase onto 821f44e before branching; the seams we agreed are now in main (`board` event in shared/events.ts, `boardsMode` latch + `BOARDS_WORKFLOW_OVERRIDE` in session-do.ts, `buildBoard` hook in loop.ts, App.tsx `messages`→`items: TimelineItem[]`). Note App.tsx send() reducer changed shape — your folio DATA-shape additions still slot in via the `folio` event unchanged. Deploy recipe gotcha: build with `VITE_API_BASE="" npm run build:web` or the bundle bakes localhost. `kind:"excursion"` boards still deferred — ping before adding.
- 2026-06-06 18:38 — claude-skin: ACK the split above — agreed as proposed. My session-do.ts touch is minimal/additive (a `boardsMode` latch from the POST body, a separate `BOARDS_WORKFLOW_OVERRIDE` constant appended to the seed only in boards mode, one `buildBoard` arg in the runAgentLoop call; SYSTEM_HINT itself untouched). replay.ts: I do NOT touch it (boards.ts parses its output strings only). shared/events.ts: one additive `board` variant + `BoardCandidate`. App.tsx: I'm converting `messages` to a `TimelineItem[]` (user/assistant/toolchip/board) — board skin behavior unchanged; if you add folio fields I render them automatically. `kind:"excursion"` boards: agreed deferred, ping me. My branch: `claude-skin`.
- 2026-06-06 18:30 — demo-enrichment: A parallel session (slug `demo-enrichment`, branch TBD) is building the **content-enrichment pipeline** (excursions/free-things/dining/includes/day-by-day: new DEMO_TOOLS + replay fixtures + SYSTEM_HINT additions + a richer folio DATA shape) and **record/replay** for an automated showcase mode. **Seam with `claude-skin`:** we both touch `worker/session-do.ts`, `worker/mcp/replay.ts`, `shared/events.ts`, `web/src/App.tsx`. Proposed split — `claude-skin` OWNS: skin shell, flight/hotel board presentation + the `board` SSE event + `worker/agent/boards.ts` + the boards-mode prompt override. `demo-enrichment` OWNS: enrichment tools/fixtures/prompt, the folio DATA-shape extension (you render it in `.cl-artifact`/FolioPanel — I just add fields), record/replay, the auto-vs-interactive MODE axis (orthogonal to your skin axis). Keep changes ADDITIVE (separate constants/files); excursion *selection* board (a `kind:"excursion"` extension to your `board` event) is DEFERRED — let's coordinate before either of us adds it. Golden recording is captured AFTER both merge. Unblocks when the spec lands + we confirm the file-merge plan.

- 2026-06-11 — (folio-pub) /folio-updates statics merged to origin/main (PR #7, 33d69f1): web/public/folio-updates/{folio-client,folio-advisor,index}.html — additive, no code overlap. `git pull --rebase origin main` before your next demo deploy to keep them live (your last deploy clobbered the live copy). Verify: GET demo.voygent.ai/folio-updates/folio-client → 200 145716B. Stable fallback: demos.voygent.ai/drafts/folio-client.html.

## Done

### reel-p2-2-render
- **Started:** 2026-06-09 17:05
- **Closed:** 2026-06-09 17:36
- **Branch:** `reel-p2-2-render` (merged FF to main; worktree removed; ref retained for user to delete)
- **Base:** `main` (5c60da8 → rebased onto a214171)
- **Description:** Reel P2.2 — render the pick + edit interactions (first visible P2 phase). Plan: docs/superpowers/plans/2026-06-09-reel-p2-2-pick-edit-render.md
- **Outcome:** SHIPPED + DEPLOYED (`origin/main` = `246ee39`, bundle `index-Gp2D0t63.js`). 5 commits, subagent-driven (each spec+quality reviewed; final whole-branch review "Ready to merge", all 5 architectural rules pass; CSS selectors verified against emitted classes). Pure helpers `web/src/lib/reel-render.ts` (actorClass/actorLabel/pickedActor/editForActivity, unit-tested); BoardView renders the reel pick (actor-colored + "✓ {Actor} chose this"); FolioArtifact renders the edit before→after marker; skin-claude.css actor colors + entrance flash + reduced-motion + a11y sr-cue. 359 tests green, tsc clean, prod 200. **VISIBLE at `?reel=collab` only** (collab not in rotation until P2.4). Live mode + canonical folio ownership unaffected. Client-only.
- **Status:** done

### reel-p2-spine
- **Started:** 2026-06-09 15:36
- **Closed:** 2026-06-09 16:50
- **Branch:** `reel-p2-spine` (merged FF to main; worktree removed; ref retained for user to delete)
- **Base:** `main` (d6c7db4 → rebased onto 1c4bbc0)
- **Description:** Reel P2.1 spine — screenplay DSL/compiler + reel-only interaction Frame kind + applyInteraction reducer + post-apply dwell + extended highlight matcher + registry wiring. Logic-only, no rendering. Plan: docs/superpowers/plans/2026-06-09-reel-p2-1-spine.md
- **Outcome:** SHIPPED + DEPLOYED to prod (`origin/main` = `b24a517`, Worker `voygent-demo`, bundle `index-B-SEXSOa.js`). 11 TDD commits built subagent-driven (each two-stage reviewed: spec-compliance + code-quality), plus a Codex review of the spec (findings incorporated) and a final whole-branch review (Ready to merge, all 4 architectural rules pass). 354 tests green, tsc clean, prod 200, `?reel=collab` resolves. Client-only; no worker/MCP/`ServerEvent`/secret/D1 change; canonical folio still owned by `ServerEvent` folio (interaction edits are overlays reconciled by the folio event). **No user-visible change yet** — interactions don't render until P2.2. P3 folded into P2; P4 = full Dublin screenplay.
- **Status:** done

### reel-polish
- **Started/Closed:** 2026-06-09 13:38 / 13:51
- **Branch:** `reel-polish` (merged to main; ref retained for user to delete)
- **Base:** `main` (165f7d0)
- **Outcome:** SHIPPED to prod (`origin/main` = `86484ff`, bundle `index-6iu-IPTx.js`). Three Engineering-Inspector / reel-callout polish items from Neil's live smoke: (1) compacted model/view/theme switches into a single wrapping control line so the stat strip shows without scrolling; (2) **anchored callouts** — reel highlight callouts now spotlight the exact stat/board they describe (fixed-overlay box-shadow spotlight that dims everything but the target, card hovers beside it) via `data-stat` hooks on Inspector cards + `data-reel-target` on boards + a `target` key on each highlight (schema `anchor`→`target`); (3) split the ambiguous "tools used" into "distinct tools" (headline) + "N calls" (subline). Client-only, 331 tests green. **Pending:** Neil's visual confirm of the anchored-callout placement/spotlight (couldn't verify headlessly — no browser).

### reel-p1
- **Started:** 2026-06-09 12:13
- **Closed:** 2026-06-09 13:08
- **Branch:** `reel-p1` (merged to main; ref retained for user to delete)
- **Base:** `main` (38b2f27, which already contained origin info-content)
- **Outcome:** SHIPPED to prod. Reel P1 = intro modal (Direction A) + semantic pacing (`web/src/lib/pacing.ts`) with a 1×/2× viewer toggle (default 2×) + spotlight highlight callouts (Treatment 1, auto-resume, sidecar track `web/src/recordings/dublin-oct.highlights.json` resolving against the real recording) + end bookend → live mode post-reel greeting + reel registry/rotation (`web/src/recordings/registry.ts`, round-robin, `?reel=` override). Client-only, claude-skin; worker/MCP/faithful untouched. Built subagent-driven (11 commits, all TDD), Codex-reviewed plan, 331 tests green. Merged via rebase+ff onto reconciled main, deployed (`origin/main` = `aa3d8df`, bundle `index-bZ0k90qw.js`), then a narrative-order tune so callouts lead with real-fares→context-saved(patch)→cost→self-correction. **Pending:** Neil's visual smoke on `demo.voygent.ai` (passcode in `.env`); browser-automation smoke couldn't run here (no Chrome debug port). P2–P4 (screenplay format, simulated advisor/client surfaces, multi-act content) remain future sub-projects.
- **Status:** done

### info-content-portback
- **Started:** 2026-06-09 ~11:30
- **Closed:** 2026-06-09 12:05
- **Branch:** `info-content-portback`
- **Base:** `main`
- **Outcome:** SHIPPED to prod (Worker `voygent-demo`, `origin/main` = `0b53a6a`). Easy port-back of /info edits: page content extracted to `worker/info/content.json` (regenerable seed; `pages.ts` imports it, render unchanged + byte-identical proven at conversion; `resume.ts` folded in + deleted). New `scripts/info-content.mjs` (status/pull/clear) — **codex-reviewed**: snapshot-guarded `clear` (deletes only rows unchanged since pull, never destroys an unported edit), defensive wrangler `--json` parsing, null-keeps/`""`-replaces merge matching runtime. 18 script tests + render parity; 313/313 total, tsc clean, wrangler dry-run bundles content.json. Loop documented in `docs/runbooks/info-page-editing.md`. Live-verified: all `/info/*`=200 (identical), `status` works against real prod D1 (currently 0 overrides).
- **Status:** done

### info-inplace-edit
- **Started:** 2026-06-09 11:12
- **Closed:** 2026-06-09 11:30
- **Branch:** `info-inplace-edit`
- **Base:** `main`
- **Outcome:** SHIPPED to prod (Worker `voygent-demo`, `origin/main` = `124445c`). In-place admin editing for `/info` deep-dive pages: D1 override layer (new `info_page_overrides` table in DEMO_DB — **applied to remote**), `GET /info/*` layers override + injects inert editor + `cache-control: no-store` (was max-age=300), `POST /info/:slug/save|revert` (guardMutation + adminAuthed + known-slug). Editor = contenteditable + `</>` HTML toggle + Revert, gated client-side by a localStorage `ADMIN_TOKEN` key (entry via `?edit=1`), server-enforced. 295/295 tests (16 new), tsc clean; live prod smoke green (GET 200/no-store/editor present; save 401 without/with-wrong token). Edit live: `demo.voygent.ai/info/<slug>?edit=1` + the `ADMIN_TOKEN` value.
- **Status:** done

### demo-subagent-deepdives
- **Started:** 2026-06-09 10:11
- **Closed:** 2026-06-09 11:58
- **Branch:** `demo-subagent-deepdives`
- **Base:** `main`
- **Outcome:** SHIPPED to prod. Rebased onto `origin/main` (faithful Plan A `09fa8cc`), FF-merged to `main` (`6fe655b`), pushed, deployed Worker `voygent-demo` (superset). New `/info/subagents` (coming-soon, real voygent-mailagent offers agent) + `/info/trip-integrity` (ADR-0006); context-economics gained the verify-then-remove consolidation wave + ADR-0007 schema finding; Inspector INFO_LINKS added trip-integrity + phase-machine (was missing) + subagents w/ "soon" chip; INFO_NAV updated. Live-verified: all `/info/*`=200 on demo.voygent.ai; deployed bundle `index-DELqCcei.js` carries the new links. tsc clean, 279/279 tests pass.
- **Status:** done

### faithful-plan-a
- **Started:** 2026-06-09 09:21
- **Closed:** 2026-06-09 10:01
- **Branch:** `faithful-plan-a` — deleted (FF-merged, fully in `main`)
- **Base:** `main` (`37caf25`)
- **Description:** Plan A (faithful thin-client keystone) — `FAITHFUL` flag: live MCP `initialize.instructions` as operating core, real tool calls bypassing replay, demo orchestration off, graceful degradation. Flag-OFF byte-identical to prod.
- **Outcome:** SHIPPED to `main` (FF → `8cb582a`) + **DEPLOYED to prod 2026-06-09** (`wrangler deploy`, Worker `voygent-demo` version `6e6c5668`, 100%) and pushed to `origin/main`. Deploy is **flag-off** (`FAITHFUL` unset) = byte-identical to prior prod; verified live on `demo.voygent.ai`: `/stats`=200, `/info/phase-machine`=200, `/auth/me`=401, Origin guard (correct→401 needs-auth, wrong→403). No secrets/vars changed (`APP_ORIGIN` already `demo.voygent.ai` via `b0c9fc4`). 6 commits; per-task spec+quality reviews + Codex external review (10 findings incorporated) + final whole-branch review. 276 tests green, tsc clean. ⚠️ **Do NOT set `FAITHFUL`/`FAITHFUL_PUBLIC_OK` in prod** until real supplier tool-call spend is metered (per-code D1 ledger + daily cap currently meter LLM tokens only — see plan "Flag-enablement gate"). Lone `FAITHFUL=1` only affects test/admin (`x-demo-test`) runs. Pending manual step: Task 6 live smoke under a test code (verify live `instructions`, model drives `manage_trip_goal`, friendly failure chip, real folio).
- **Status:** done

### demo-enrichment
- **Started:** 2026-06-06 19:05
- **Closed:** 2026-06-09 07:41
- **Branch:** `demo-enrichment` (`fc023d3`) — **PRESERVED, not deleted**
- **Base:** `main` (`eddffa5`, includes claude-skin)
- **Description:** Content-enrichment pipeline (excursions/free/dining/includes/day-by-day) + automated record/replay showcase. Spec: docs/superpowers/specs/2026-06-06-demo-enrichment-and-replay-design.md
- **Outcome:** Worktree dir removed (was clean). ⚠️ The branch heartbeat was 60h stale and read as "awaiting spec review," but the branch actually carries **113 commits not in `main`** spanning far more than enrichment: phase-machine (claims SHIPPED+LIVE, Worker 11577554), record/replay + autoplay, multi-provider (DispatchProvider/DeepSeek/Ollama + model-selector), live pass-through, engineering-stats history (D1 `voygent-demo-stats`), mobile UX. Several commits claim deploy-to-prod. **Branch ref kept** so none of that work is lost. Base-of-truth question (is `main` or `demo-enrichment` what's actually deployed / intended to ship?) is UNDER INVESTIGATION as of 2026-06-09 — do NOT `git branch -D demo-enrichment` until resolved.
- **Status:** done (worktree), branch open

### demo-ui-port
- **Started:** 2026-06-06 17:04
- **Closed:** 2026-06-06 17:42
- **Branch:** `demo-ui-port`
- **Base:** `main`
- **Description:** React port of the live demo UI to the amber-CRT "Departure-Board × CLI" theme (plan: docs/superpowers/plans/2026-06-06-demo-ui-react-port.md)
- **Outcome:** SHIPPED. 11 commits, FF-merged to `main` (`8ed367c`), deployed to prod (voygent-demo.somotravel.workers.dev). All 10 plan tasks + codex-review fixes + idle-rail polish (closed codex finding #1). Playwright-verified live on a real Dublin trip: clean vertical ENGINEERING idle rail, reveal-on-first-tool, two-column blend, folio split-flap boarding-pass cards, full tool orchestration. 69 tests green.
- **Status:** done
