# Auto Session Pause
**Date:** 2026-06-10 at 17:31
**Repo:** /home/neil/dev/voygent-demo
**Session:** e6aec0d8
**Branch:** main
**Uncommitted changes:** yes

## Git State
```
?? docs/summaries/pause-2026-06-10-470e6ff2.md
?? docs/summaries/pause-2026-06-10-4b310626.md
```

## Recent Changes
```
(none)
```

## Recent Commits
```
01c8629 docs(session-log): cpmaxx hotels + inspector rail & surface polish
27e4ff0 docs(handoff): inspector polish phases A-C shipped; phase D + C5/C7 + multi-select pickup
31cc025 docs(journal): phase C skinny live rail shipped (bundle D4HAjykg)
a26a54c feat(demo): skinny live Inspector rail (idle/peek/open) + registry-driven rail + dig-deeper links
1b0a026 feat(demo): inspector stat registry (buildStats/railStats/deepDiveLinks)
```

## Last Conversation Context
**Assistant:** This gives me the full picture. The open panel is a stack of `ins-region` sections, the tiles are still hardcoded `ins-strip` divs (C5 not done), and crucially **the Inspector already receives all the per-turn / per-tool / per-model data** Phase D needs via its props (`turns`, `summaries`, `tools`, `savings`, `overhead`). The one thing I need to confirm before finalizing scope is the single noted data gap: does the **raw→slim per-search token pair** (for the Token Elimination Funnel) already rea...

**Assistant:** Let me see exactly how the `searchDistill` savings event is built — whether raw and slim tokens are both in hand at emit time (which decides if the funnel needs a new event field).

**Assistant:** I now have the complete data picture. The key finding for planning:

**The one data gap is tiny.** Every Phase D view except the funnel draws on data the Inspector *already* receives as structured props (`turns[]` → token/model/cost per turn, `summaries[]` → costByModel + actualCost, `tools[]` → latency/raw/slim). The **Token Elimination Funnel** wants a per-search `raw → slim` token pair — and those exact numbers (`meta.rawTokensEst`, `m.modelFacingTokens`) are already in hand at emit time in `...

**Assistant:** All three it is — codex top-3 as Phase D v1. One more fork that shapes the whole structure: **how the three views attach to the panel.** Two of them tie naturally to existing stat tiles (funnel → "context kept out," cost sim → "observed cost"), but the waterfall doesn't map to a single number — it's an orchestration view. This decides whether we fold in C5 (registry-driven, clickable tiles) as the foundation.

**Assistant:** Both forks decided: **all three views**, attached via **drillable registry-driven tiles** (folding in C5). Let me confirm one data detail for the cost simulator before I present — whether per-model rate constants are reachable client-side (decides if the "no-cache" scenario is free or needs plumbing).

**Assistant:** Key constraint found: `worker/inspector.ts:50` — **"client never holds pricing"** is a deliberate principle (rates live server-side in `llm/cost.ts`). That shapes the cost simulator: scenarios needing *repricing* (no-cache, no-distill) can't be computed client-side without either a new server event or breaking that principle. The three scenarios already on the client (actual routed, all-Sonnet, all-Opus) need zero plumbing. I'll spec v1 around those and flag the repricing scenarios as a document...

## Instructions
Continue the work from this session. Review the git state and conversation
context above to understand where things left off. Ask the user what to
focus on next if the direction isn't clear from context.
