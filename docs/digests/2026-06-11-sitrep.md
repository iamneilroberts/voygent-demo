# /sitrep — 2026-06-11

**Status:** OK
**Window:** 2026-05-28 → 2026-06-11 · **Continuity lag:** 18d (chat-history index ends 2026-05-24; all June claims rest on git/prod/journals/memory, not transcripts)

Sweep note: the voygent MCP tools were broken this session (renamed, unrestarted). The internal issue store was read **directly from prod KV** (`_issues/` + `_issues_index/by_date/` walk, replicating `scanMyIssues`), so the issues source is covered, not a gap.

## 1. Shipped vs claimed

### CONTRADICTED (look here first)
- None. The curator found no claim contradicted by ground truth this sweep.

### Unverified
- **Pro-tier 503 fail-closed in prod** — code-verified (tier.ts `pickBearer`/`resolveLiveBearer`, session-do.ts:409-411 returns 503; `VOYGENT_MCP_BEARER_PRO` absent from secret list; 7/7 tier tests) but **no granted pro code has ever exercised the path in prod**. One-click check available: grant the pending "Smoke Test Pro" request in /admin, auth with the minted code, expect 503. · confidence m · 2026-06-11
- **Live supplier-raw searchDistill event** (tokensSaved 5948, ~27476B→~3682B, BOS→Porto) — deploy surface VERIFIED (f0b89e7 ∈ origin/main, live bundle `DbMSEFYq`); the event itself is session-attested (re-running costs money). Arithmetic independently checks (`6869−921=5948`). · confidence m-h · 2026-06-11
- **Demo bearer secret VALUE = the ADR-0008 credential-free identity** — secret name presence + cross-repo curator attestation (voygent-lite journal L274: 32 free tools, zero credentialed, cpmaxx denied); value itself unreadable by design. · confidence m-h · 2026-06-11
- **Promotion-GO freshness caveat** — the headless funnel re-smoke ran against bundle `mUB0ysTN`; two deploys (`S1R7yA2g` claude-on-phones, `DbMSEFYq` telemetry) landed since. Neil's browser smoke covers newer code than the re-smoke did. · 2026-06-11

### Verified shipped (majors, window 05-28 → 06-11)
**voygent-demo (repo created 2026-06-05 — the whole product shipped inside this window):**
- Agent host + fixture-replay anti-fabrication + Inspector v1 + amber/claude skins + enrichment/replay — 06-05/06-06
- Multi-provider LLM routing, phase machine, D1 stats history, live pass-through + advisor view — 06-07
- Access control (passcode/budgets/admin, PRs #2–#4) + demo.voygent.ai + trunk reconciliation + FAITHFUL flag — 06-09
- Reel program complete twice over (P1–P4 then R1–R5 rewrite + transport/scrubber/end-state) — 06-09/06-10, `5e5e27e`
- Auth+onboarding redesign DEPLOYED (open reel → self-serve public codes → pro request/grant; PR #6, merged 06-11) — 06-10
- Inspector Phases A–D + aggressive declutter (6 drills) + funnel-noise/chip polish — 06-10, `9edb7e8`/`216c19b`
- Public default = orchestrated replay (`?faithful=1` opt-in); cpmaxx hotel captures wired into live boards — 06-10
- Public-funnel re-smoke on the new bearer (promotion GO pending Neil) `a15f07a`; claude skin on phones `24c5076`; live supplier-raw telemetry in the Inspector `f0b89e7` (curator: both Inspector hard laws honored) — 06-11
**voygent-lite:**
- Lodging model M1–M4 + 180-trip migration; folio-board program (vNext A/B, badges, inline edit, annotation emails) — 05-30→06-03
- Trip-fill loop v1 (`manage_trip_goal`) 06-03; Trip Hero ph1–3 06-04; AE telemetry P1 06-04
- /onboard probe-brain rebuild (643 assertions, drops desktop dep) `2bcda5b` 06-05; farebuzz premise FALSIFIED (merged-inert) 06-05
- Prompt thin-core (91% cut) + server-driven build loop + PR #172 clientUrl fix 06-08
- Schema-opt program: M1 fan-out routers (PRs #167/#170) + M2 action routers #1–#6 (PRs #182–#189) + ADR-0007 intent routing — 06-04→06-09
- ADR-0008 credential-free demo bearer shipped + handed over (PR #191) 06-11; supplier-raw size telemetry Phase A (PR #171, `2807ae09`) 06-11

## 2. Milestone-anchored next steps
MILESTONES.md is the priority authority (last updated 2026-05-28 — see drift #1). /focus discipline, M0→M3 first-unchecked:

- **Primary — M0 (dev/prod separation, BLOCKING, target 2026-06-01 = 10 days SLIPPED):** Decide environment topology — separate Worker (`voygent-lite-dev`) vs separate route on same Worker. Neil-only decision; everything after it is 2–3 days of execution. Related open internal issue: `velvet-keystone` (git-sync + traffic toggle).
- **Backfill — M1 (tiers + OAuth, target 2026-06-15 — 4 days out, at risk):** Lock the tool list per tier in writing (MILESTONES + `src/mcp/server.ts` comment block). Note: ADR-0008 already locked the *demo free surface* (32 tools) in writing, and the router consolidation rebuilt the catalog — this action is now substantially easier than when written.
- **Demo promotion (cross-cutting, this repo):** GO pending Neil's in-browser eyeball + grant/deny of the pending smoke pro-request (which doubles as the missing prod fail-closed verification). Not on MILESTONES — see drift #1.

## 3. Off-roadmap drift (max 3, propose-only)
- **The entire voygent-demo program** (~2 weeks: reel, Inspector, auth funnel, promotion-ready demo) — impact: it IS a working top-of-funnel + lead capture (pro-request → Neil's inbox), i.e. de-facto M3 work that MILESTONES doesn't know about — tag: `roadmap-it` (fold demo promotion into M3 as the funnel's first artifact)
- **mcp-schema-optimization program** (fan-out routers, M2 action routers, ADR-0007/0008) — impact: directly advances M1's catalog-refactor/tier-gating actions and M2 connector schema quality, but its "M1/M2" labels collide with roadmap M0–M3 naming and can masquerade as roadmap progress — tag: `roadmap-it` (credit it under M1's catalog actions; rename the plan's phase labels)
- **Open `important` internal bugs (13 of 33 open issues)** — esp. `sharp-plateau` (proxied tools double-wrap MCP envelope), `plucky-oasis` (preview_publish serves stale HTML after patch_trip), `fleet-thicket` (day-11 tips/suggestions don't render), `umber-plateau` (preview_publish↔update_advisor_profile schema mismatch), onesource trio — impact: advisor-facing correctness on Kim's daily workflow — tag: `consciously-defer-until-2026-06-15` (post-M1 lock; revisit at the Friday review)

## 4. Proposed MILESTONES.md diff
PROPOSED — apply only on approval. Status + checkbox lines only; no checkbox flips are evidence-justified this sweep (the shipped tier/catalog work is adjacent to, not identical with, M1's written actions).

```diff
--- a/docs/roadmap/MILESTONES.md
+++ b/docs/roadmap/MILESTONES.md
@@ M0 — Dev/prod separation
-- **status:** not started
+- **status:** not started — SLIPPED (target 2026-06-01 passed; window went to voygent-demo + schema-opt; trigger Friday review: cut scope or move date)
```
Rationale: M0's date is a commitment per the file's own operating discipline; 10 days of silent drift is exactly what rule 4 forbids. No other status line is false: M1 "in_progress" remains true (ADR-0007/0008 + router consolidation are real M1-adjacent progress), M2/M3 "not started" remain true (the demo funnel is real but is not the landing page).

## 5. Sweep completeness
- Loaded: git log + journals (both repos), worktree-journal Coordination (both), MILESTONES.md, docs/summaries handoffs (incl. credential-free-bearer + supplier-raw), docs/superpowers specs, ADR-0007/0008 evidence, GitHub issues/PRs (`gh`, both repos), **internal issue store via direct prod KV read** (331 records; 52 in 30-day index; 33 open/in_progress, 13 important), vestige (facts/decisions/intentions), project auto-memory, continuity-v2 index stats, live prod reads (bundle, secret list), curator verification pass (4 claims).
- DATA GAPS: continuity-v2 transcripts post-2026-05-24 (18d lag — June work has zero chat-history corroboration; historical lane degraded gracefully). voygent MCP tools (renamed, unrestarted session) — bypassed via direct KV/gh reads, no content gap.
- Curator corrections folded in: ADR-0008 journal pointer is voygent-lite L274 (not L14-15); pro-503 code pointer is session-do.ts:409-411 (post-f0b89e7 line shift).
